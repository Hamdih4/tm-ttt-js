import React, { Component } from 'react';
import Game from './game';

export default class Menu extends Component {
  render() {
    return (
      <div className="menu">
        <Game />
      </div>
    );
  }
}
